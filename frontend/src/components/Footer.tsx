import React from 'react';
import styles from "../styles/Footer.module.css"

const Footer: React.FC = () => {
  return (
    <footer className={styles.footer}>
      <p>Reimbursement App</p>
    </footer>
  );
};

export default Footer;
