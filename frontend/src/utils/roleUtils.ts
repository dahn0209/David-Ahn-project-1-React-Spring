// export const isManager = (role: string): boolean => {
//     return role === 'MANAGER';
//   };
  
//   export const isEmployee = (role: string): boolean => {
//     return role === 'EMPLOYEE';
//   };
  
//   export const canProcessTickets = (role: string): boolean => {
//     return isManager(role);
//   };
export {}